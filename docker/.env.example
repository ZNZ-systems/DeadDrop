PORT=8080
DATABASE_URL=postgres://deaddrop:deaddrop@db:5432/deaddrop?sslmode=disable
BASE_URL=http://localhost:8080
SECURE_COOKIES=false
# Outbound mail defaults to bundled local relay container.
SMTP_ENABLED=true
SMTP_HOST=smtp-relay
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
SMTP_FROM=deaddrop@localhost
# Optional smarthost relay for networks that block direct port 25 egress.
RELAYHOST=
RELAYHOST_USERNAME=
RELAYHOST_PASSWORD=
RATE_LIMIT_RPS=2
RATE_LIMIT_BURST=5
SESSION_MAX_AGE_HOURS=72
